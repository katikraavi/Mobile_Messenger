# Tasks: Initialize Flutter Messenger Project

**Input**: Design documents from `/specs/001-messenger-init/`  
**Prerequisites**: plan.md ✓, spec.md ✓, research.md ✓, data-model.md ✓, contracts/ ✓, quickstart.md ✓

**Format**: `- [ ] [ID] [P?] [Story?] Description with file path`

**Organization**: Tasks grouped by user story for independent implementation and testing. Each story can be developed, tested, and delivered independently.

---

## Phase 1: Setup (Project Initialization)

**Purpose**: Initialize monorepo structure and prepare shared configuration files

**⏱️ Estimated**: 0.5 hours | **🎯 Parallelizable**: Yes

- [ ] T001 [P] Create Flutter frontend project scaffold at `/frontend` with `flutter create --org com.messenger frontend && cd frontend && flutter pub get`
- [ ] T002 [P] Create Serverpod backend project scaffold at `/backend` with `serverpod create --org com.messenger --app-name messenger backend`
- [ ] T003 [P] Create project root `.gitignore` file with Flutter, Dart, Docker, IDE patterns at `/.gitignore`
- [ ] T004 [P] Create `.env.example` file with template environment variables at `/.env.example`
- [ ] T005 [P] Create initial `README.md` at repository root (placeholder for quickstart content)
- [ ] T006 Verify both projects created successfully by checking directory structure exists:
  - `/frontend/lib/main.dart`
  - `/frontend/pubspec.yaml`
  - `/backend/lib/server.dart`
  - `/backend/pubspec.yaml`

**Checkpoint**: Project structure initialized; both frontend and backend projects exist with default scaffolding

---

## Phase 2: Foundational (Blocking Prerequisites - Infrastructure)

**Purpose**: Docker orchestration, database setup, environment configuration that ALL features depend on

**⏱️ Estimated**: 1.5 hours | **🎯 Parallelizable**: Partial

**⚠️ CRITICAL**: This phase MUST complete before any user story implementation begins

- [ ] T007 [P] Create `docker-compose.yml` at repository root with PostgreSQL 13 and Serverpod services as specified in research.md
  - PostgreSQL service: port 5432, volume `postgres_data:/var/lib/postgresql/data`
  - Serverpod service: depends_on PostgreSQL, port 8081, environment `DATABASE_URL`
  - Health checks on both services

- [ ] T008 [P] Create `backend/Dockerfile` for Serverpod container build
  - Base image: `dart:3.0-stable`
  - Copy backend code, run `pub get`, expose port 8081

- [ ] T009 [P] Create `backend/.env` file with initial values (or update `docker-compose.yml` env template)
  - `DATABASE_URL=postgres://messenger_user:messenger_password@postgres:5432/messenger_db`
  - `SERVERPOD_ENV=development`
  - `SERVERPOD_PORT=8081`

- [ ] T010 Update `backend/lib/server.dart` entry point configured for Serverpod 2.1+
  - **Note**: `serverpod create` auto-generates this file; this task updates it to add database connection and endpoint registration
  - Ensure Server initialized with Protocol()
  - Connect to PostgreSQL via DATABASE_URL environment variable
  - Register health endpoint (health.dart created in T015)
  - Start listening on port 8081

- [ ] T011 Create `backend/pubspec.yaml` dependencies for Serverpod scaffold
  - serverpod: ^2.1.0
  - postgres package for database connection
  - cryptography for encryption support (per Constitution)

- [ ] T012 Update `frontend/pubspec.yaml` dependencies
  - flutter, cupertino_icons (default)
  - http for API calls to Serverpod
  - Add any state management packages (riverpod, bloc, provider, etc. - defer to future)

- [ ] T013 Create `backend/migrations/` directory for Serverpod auto-generated migrations
  - Directory exists but empty initially; migrations auto-generated by Serverpod on schema changes

- [ ] T014 Verify Docker Compose configuration works with `docker-compose config` command (syntax check)

**Checkpoint**: Infrastructure ready - Docker Compose can orchestrate both services; database prepared for migrations

---

## Phase 3: User Story 1 - Developer Sets Up Local Development Environment (Priority: P1) 🎯 MVP

**Goal**: Developer can run `docker-compose up` once and have a fully operational backend + database + Flutter app that connects successfully within 2 minutes. Health check endpoint responds; app displays in emulator without errors.

**Independent Test**: 
- Run `docker-compose up` in project root → backend and PostgreSQL start within 30 seconds
- Curl `http://localhost:8081/health` → returns 200 with JSON `{"status": "ok", ...}`
- Launch Flutter app on Android emulator → app displays, logs show "Backend connected"

### Implementation for User Story 1

- [ ] T015 [P] [US1] Create `backend/lib/src/endpoints/health.dart` endpoint implementing `GET /health`
  - Returns JSON: `{"status": "ok", "timestamp": "<ISO8601>", "uptime_ms": <milliseconds>}`
  - Responds with 200 status code always (in this feature; future features may add service checks)
  - File named `health.dart` (not `health_endpoint.dart`) for consistency with other endpoint files
  - Use Serverpod request/response model from contracts/health-check.md

- [ ] T016 [P] [US1] Verify `backend/pubspec.yaml` entry point configuration
  - **Note**: `serverpod create` auto-generates pubspec.yaml; this task verifies it correctly references `backend/lib/server.dart`
  - Verify `pubspec.yaml` name field: `name: messenger` or `name: messenger_server`
  - Verify Dockerfile will run correct Serverpod entry point command

- [ ] T017 [P] [US1] Create `frontend/lib/main.dart` with app initialization
  - Import Flutter Material design
  - Define MessengerApp as root widget
  - Pass backend URL configuration (from environment or constants)
  - Call apiClient initialization before runApp

- [ ] T018 [P] [US1] Create `frontend/lib/core/services/api_client.dart`
  - HttpClient for connecting to backend
  - Initialize with base URL (http://host.docker.internal:8081 for Android, http://localhost:8081 for iOS)
  - Implement `isHealthy()` method that calls `GET /health`
  - Implement `connectToBackend()` with exponential backoff retry logic (5 attempts: 100ms, 500ms, 2s, 5s, 10s)

- [ ] T019 [P] [US1] Create `frontend/lib/app.dart` root widget
  - Define MessengerApp as StatelessWidget
  - Setup Material navigation
  - Call api_client.connectToBackend() on app start
  - Display loading/error UI while connecting
  - Show connection status in logs

- [ ] T020 [US1] Update `backend/lib/server.dart` to register health endpoint
  - Import `src/endpoints/health.dart` (file named `health.dart` per T015)
  - Register HealthEndpoint in server configuration
  - Ensure endpoint responds at `/health` path

- [ ] T021 [US1] Create test script `verify_setup.sh` to validate complete setup
  - Check docker-compose.yml syntax
  - Verify directories exist: /frontend, /backend
  - Verify files exist: docker-compose.yml, Dockerfile, pubspec.yaml files
  - Document in quickstart.md how to verify

- [ ] T022 [US1] Test Docker Compose startup
  - Run `docker-compose up` from repository root
  - Verify PostgreSQL container starts and "ready to accept connections"
  - Verify Serverpod container starts and logs show "Serverpod started on port 8081"
  - Verify health endpoint responds: `curl http://localhost:8081/health`
  - Run `docker-compose down -v` to clean up test

- [ ] T023 [US1] Test Flutter app connection
  - Launch Android emulator or iOS simulator
  - Run `flutter run` from /frontend directory
  - Verify app displays without errors
  - Verify logs show connection attempt and success ("Backend connected")
  - Verify app shows main UI or home screen placeholder
  - Stop with Ctrl+C

**Checkpoint**: User Story 1 Complete ✅
- Backend starts in 30 seconds: `docker-compose up`
- Health endpoint responds: `curl http://localhost:8081/health`
- Flutter app launches and connects: `flutter run`
- New developer can complete setup in <15 minutes following quickstart.md

---

## Phase 4: User Story 2 - Backend Developer Creates Endpoints (Priority: P2)

**Goal**: Backend developer has clear, organized directory structure for creating endpoints for auth, profile, chat, and invites features. Service organization is in place; patterns for creating new endpoints are established and documented.

**Independent Test**:
- Directory structure exists: `/backend/lib/src/endpoints/` with subdirectories or files for auth, profile, chat, invites
- Directory structure exists: `/backend/lib/src/services/` ready for business logic
- Directory structure exists: `/backend/lib/src/models/` ready for data models
- Developer can create new endpoint following established patterns without guidance

### Implementation for User Story 2

- [ ] T024 [P] [US2] Create backend endpoint directory structure at `/backend/lib/src/endpoints/`
  - Placeholder files: `auth.dart`, `profile.dart`, `chat.dart`, `invites.dart`
  - Each contains minimal endpoint stub: `class AuthEndpoint extends Endpoint { ... }`
  - health.dart already created in US1

- [ ] T025 [P] [US2] Create backend services directory structure at `/backend/lib/src/services/`
  - Placeholder files: `auth_service.dart`, `profile_service.dart`, `chat_service.dart`, `invite_service.dart`
  - Each contains minimal service class: `class AuthService { ... }`
  - Services follow naming convention from data-model.md

- [ ] T026 [P] [US2] Create backend models directory structure at `/backend/lib/src/models/`
  - Placeholder files: `user_model.dart`, `message_model.dart`, `invite_model.dart`, `chat_model.dart`
  - Each contains minimal model class extending `UpdateModel` (Serverpod)
  - Models follow naming convention from data-model.md
  - **TODO** (deferred to auth/profile/chat/invites features): Add encrypted field examples per Constitution I (Security-First). See data-model.md encryption policy for pattern (encryptedFieldName prefix)

- [ ] T027 [US2] Create `backend/ENDPOINT_PATTERNS.md` documentation
  - Document how to create new endpoint (inherit from Endpoint, register in server.dart)
  - Document how to inject services (dependency injection pattern)
  - Example: Create a GET endpoint that returns data from a service
  - Link to Serverpod documentation

- [ ] T028 [US2] Create `backend/SERVICE_PATTERNS.md` documentation
  - Document service class structure
  - Document how to use models in services
  - Document error handling pattern (throw ServiceException)
  - Show example service method

- [ ] T029 [US2] Create `backend/MODEL_PATTERNS.md` documentation
  - Link to data-model.md for organization philosophy
  - Show Serverpod UpdateModel inheritance pattern
  - Show field naming conventions (camelCase properties)
  - Show encryption field naming pattern (encryptedFieldName)

- [ ] T030 [US2] Verify all directories and placeholder files exist with `ls -R /backend/lib/src/`

**Checkpoint**: User Story 2 Complete ✅
- Backend has organized endpoint, service, model directories
- Developer can navigate structure and understand where to add new endpoints
- Documentation explains patterns and examples for endpoint creation
- Backend developer can proceed with auth feature development independently

---

## Phase 5: User Story 3 - Frontend Developer Builds UI Features (Priority: P2)

**Goal**: Frontend developer has clear, organized directory structure for building feature modules (auth, profile, chat, invites). Feature organization is in place; core utilities are available; widget patterns are established and documented.

**Independent Test**:
- Directory structure exists: `/frontend/lib/features/` with subdirectories for auth, profile, chat, invites
- Directory structure exists: `/frontend/lib/core/` with models, services, widgets, utils subdirectories
- Developer can create new feature following established patterns without guidance
- All core import structures work (e.g., `import 'package:frontend/core/models/...'`)

### Implementation for User Story 3

- [ ] T031 [P] [US3] Create frontend feature directory structure at `/frontend/lib/features/`
  - Directories: `auth/`, `profile/`, `chat/`, `invites/`
  - Each feature directory contains: `screens/`, `widgets/`, `models/`, `providers/` (if using provider pattern)
  - Placeholder main.dart in each feature showing import structure

- [ ] T032 [P] [US3] Create frontend core directory structure at `/frontend/lib/core/`
  - `models/` - shared data types
  - `services/` - contains api_client.dart (T018) + state management (defer)
  - `widgets/` - reusable widgets
  - `utils/` - constants, helpers
  - `config/` - configuration, app constants

- [ ] T033 [P] [US3] Create `frontend/lib/core/models/` with placeholder types
  - Empty models directory structure; actual models deferred to auth/profile/chat/invites features
  - Create `models.dart` barrel export file (imports all models)

- [ ] T034 [P] [US3] Create `frontend/lib/core/utils/constants.dart`
  - `const String appName = 'Mobile Messenger';`
  - `const String backendUrl = 'http://host.docker.internal:8081';` (Android) or `http://localhost:8081` (iOS)
  - Define timeout durations, retry counts, app colors/themes

- [ ] T035 [P] [US3] Create `frontend/lib/core/widgets/` with base widget components
  - Placeholder: `app_scaffold.dart` - reusable scaffold for all screens
  - Placeholder: `loading_widget.dart` - show loading state
  - Placeholder: `error_widget.dart` - show error messages

- [ ] T036 [US3] Create `frontend/lib/app.dart` with navigation setup
  - Update MaterialApp to use feature-based navigation
  - Setup route names: '/home', '/auth', '/profile', '/chat', '/invites'
  - Add named route builder for each feature (defer implementation to features)

- [ ] T037 [US3] Create `frontend/FEATURE_PATTERNS.md` documentation
  - Document feature directory structure (screens/, widgets/, models/, providers/)
  - Document how to create new screen (StatefulWidget or ConsumerWidget if Riverpod)
  - Document how to create feature widget
  - Link to Flutter best practices
  - Show example feature module

- [ ] T038 [US3] Create `frontend/lib/features/auth/main.dart` (placeholder)
  - Document auth feature structure
  - Placeholder screens/models/widgets for future auth implementation

- [ ] T039 [US3] Create `frontend/lib/features/profile/main.dart` (placeholder)
  - Document profile feature structure
  - Placeholder screens/models/widgets for future profile implementation

- [ ] T040 [US3] Create `frontend/lib/features/chat/main.dart` (placeholder)
  - Document chat feature structure
  - Placeholder screens/models/widgets for future chat implementation

- [ ] T041 [US3] Create `frontend/lib/features/invites/main.dart` (placeholder)
  - Document invites feature structure
  - Placeholder screens/models/widgets for future invites implementation

- [ ] T042 [US3] Verify all directories and files exist with `ls -R /frontend/lib/`

**Checkpoint**: User Story 3 Complete ✅
- Frontend has organized feature modules (auth, profile, chat, invites)
- Core utilities and shared components in place
- Documentation explains feature creation patterns
- Frontend developer can proceed with auth feature development independently
- Frontend and backend developers can work in parallel

---

## Phase 6: User Story 4 - DevOps Engineer Manages Infrastructure (Priority: P2)

**Goal**: DevOps engineer has complete Docker Compose setup with proper health checks, volume persistence, environment configuration, and documentation for deployment across environments.

**Independent Test**:
- `docker-compose.yml` fully configured with health checks
- PostgreSQL volumes persist data (test by stopping container and restarting)
- Environment variables correctly connect backend to database
- `docker-compose up` starts all services; `docker-compose down -v` cleans up
- Setup takes <15 minutes following documentation

### Implementation for User Story 4

- [ ] T043 [US4] Update `docker-compose.yml` with complete PostgreSQL health check configuration
  - Health check: `pg_isready -U messenger_user` every 10s
  - Service marked healthy after 1 successful check
  - Condition: `service_healthy` for Serverpod depends_on

- [ ] T044 [US4] Update `docker-compose.yml` with complete Serverpod health check configuration
  - Health check: `curl -f http://localhost:8081/health` every 10s
  - start_period: 10s (wait before first check)
  - Retries: 3 (fail after 3 consecutive failures)

- [ ] T045 [US4] Create `backend/.docker/.environment` file (or include in docker-compose.yml)
  - Document all environment variables: DATABASE_URL, SERVERPOD_ENV, SERVERPOD_PORT
  - Show example values for dev, staging, production

- [ ] T046 [US4] Create `docker-compose.override.yml` (optional, for local dev customizations)
  - Override port mappings if defaults conflict
  - Override volumes for local debugging

- [ ] T047 [US4] Document PostgreSQL volume persistence in project docs
  - Volume name: `postgres_data`
  - Mounted at: `/var/lib/postgresql/data`
  - Commands to backup: `docker-compose exec postgres pg_dump -U messenger_user -d messenger_db > backup.sql`
  - Commands to restore: `docker-compose exec postgres psql -U messenger_user -d messenger_db < backup.sql`

- [ ] T048 [US4] Document environment variable configuration
  - `.env.example` at repository root shows template
  - Document how to set `.env` for local/dev/staging/production
  - Explain what each variable does

- [ ] T049 [US4] Create `deployment/docker-compose.prod.yml` (optional, for production reference)
  - Show how to deploy to production environment
  - Explain differences from dev setup (no volume persistence for containers, external postgres, etc.)

- [ ] T050 [US4] Create `infrastructure/README.md` or document in main README.md
  - How to start services: `docker-compose up`
  - How to stop services: `docker-compose down`
  - How to clean up data: `docker-compose down -v`
  - How to view logs: `docker-compose logs -f serverpod`
  - How to restart a service: `docker-compose restart serverpod`
  - How to scale services (if applicable)

- [ ] T051 [US4] Test complete docker-compose workflow
  - `docker-compose up` → wait 30-60s for startup
  - `docker ps` → verify both containers running and healthy
  - `curl http://localhost:8081/health` → verify endpoint responds
  - `docker-compose stop` → verify services stop gracefully
  - `docker-compose start` → verify services restart; data persists
  - `docker-compose down` → verify containers removed; volume persists
  - `docker-compose down -v` → verify volume deleted

**Checkpoint**: User Story 4 Complete ✅
- Docker infrastructure fully configured and documented
- Health checks enable automated orchestration monitoring
- Volume persistence works correctly for data durability
- Environment configuration flexible for dev/staging/production
- DevOps engineer can deploy and manage infrastructure independently

---

## Phase 7: Polish & Final Verification

**Purpose**: Complete documentation, verify all acceptance criteria, prepare for deployment

**⏱️ Estimated**: 1 hour | **🎯 Deliverables**: Complete, documented, tested project

- [ ] T052 Complete `README.md` at repository root
  - Project overview
  - Tech stack summary (Flutter, Serverpod, PostgreSQL, Docker)
  - Quick start (reference to quickstart.md)
  - Directory structure overview
  - How to contribute (branch naming, PR process)
  - Links to feature documentation (FEATURE_PATTERNS.md, ENDPOINT_PATTERNS.md, etc.)

- [ ] T053 Update `/frontend/README.md` with frontend-specific documentation
  - Project structure explanation
  - How to run locally (`flutter run`)
  - How to run tests (`flutter test`)
  - How to build APK (`flutter build apk`)
  - Feature module guide (link to FEATURE_PATTERNS.md)

- [ ] T054 Update `/backend/README.md` or create with backend-specific documentation
  - Project structure explanation
  - How to run locally (docker-compose)
  - How to run tests (`pub test` or `dart test`)
  - Endpoint creation guide (link to ENDPOINT_PATTERNS.md)
  - Service layer documentation (link to SERVICE_PATTERNS.md)
  - Migration documentation (link to data-model.md)

- [ ] T055 Create `TROUBLESHOOTING.md` or add section to README.md
  - Include common issues from quickstart.md (ports in use, Docker not found, emulator issues)
  - Debugging tips (logs, health check testing, database inspection)
  - Recovery procedures (reset database, clear caches, rebuild containers)

- [ ] T056 Verify all Functional Requirements (FR-001 through FR-010) met
  - [ ] FR-001: Flutter project in `/frontend` with `lib/features/` modules ✓
  - [ ] FR-002: Serverpod backend in `/backend` with endpoint modules ✓
  - [ ] FR-003: Docker Compose configuration at repository root ✓
  - [ ] FR-004: Environment configuration for backend ↔ PostgreSQL ✓
  - [ ] FR-005: PostgreSQL with volume persistence + Serverpod auto-migrations ✓
  - [ ] FR-006: Health endpoint: `GET /health` with JSON response ✓
  - [ ] FR-007: Flutter app with retry logic ✓
  - [ ] FR-008: Monorepo structure: `/frontend`, `/backend`, `docker-compose.yml` at root ✓
  - [ ] FR-009: `.gitignore`, config files, documentation ✓
  - [ ] FR-010: Frontend core modules at `/frontend/lib/core/` ✓

- [ ] T057 Verify all Success Criteria (SC-001 through SC-008) met
  - [ ] SC-001: `docker-compose up` → services operational in <2 minutes
  - [ ] SC-002: `/health` endpoint responds in <30 seconds from startup
  - [ ] SC-003: Flutter app launches and connects without errors
  - [ ] SC-004: Frontend structure complete with core + feature directories
  - [ ] SC-005: Backend structure complete with endpoints + services + models
  - [ ] SC-006: PostgreSQL accessible from backend without manual config
  - [ ] SC-007: New developer setup <15 minutes with quickstart.md
  - [ ] SC-008: Monorepo structure follows conventions with clear separation

- [ ] T058 Verify all User Story Acceptance Scenarios pass
  - [ ] US1 Scenarios: docker-compose up works, health endpoint responds, Flutter app connects
  - [ ] US2 Scenarios: Backend structure exists, dev can create endpoints
  - [ ] US3 Scenarios: Frontend structure exists, dev can create features
  - [ ] US4 Scenarios: Docker health checks work, PostgreSQL persists, env vars correct

- [ ] T059 Final integration test: Complete flow from blank project to running app
  - Clone repository (fresh checkout)
  - Follow quickstart.md (15-minute goal)
  - Run `docker-compose up` → wait 30-60 seconds
  - Verify health check: `curl http://localhost:8081/health`
  - Launch Flutter app on emulator: `cd frontend && flutter run`
  - Verify app displays and shows connection success
  - Record time taken; verify <15 minutes

- [ ] T060 Create DEPLOYMENT_CHECKLIST.md for future feature releases
  - Pre-deployment checks (tests pass, builds succeed, health endpoints respond)
  - Deployment steps (git tag, docker build, docker-compose deploy)
  - Post-deployment verification (health checks, smoke tests, monitoring)

- [ ] T061 Create build script and documentation for Android APK delivery
  - Document Android build process: `cd frontend && flutter build apk --split-per-abi`
  - Create `frontend/build_and_deliver_apk.sh` script for one-command APK generation
  - APK output location: `frontend/build/app/outputs/flutter-apk/app-release.apk`
  - Add instructions to `frontend/README.md` for reviewers to build and test APK locally
  - **Note**: Fulfills Constitution V (Delivery Readiness) requirement to provide Android APK for reviewers

**Final Checkpoint**: Feature 001 Complete ✅

All tasks delivered:
- ✅ Flutter project initialized and running
- ✅ Serverpod backend initialized and running  
- ✅ PostgreSQL database configured with persistence
- ✅ Docker Compose orchestration working
- ✅ Health check endpoint implemented
- ✅ Flutter app connecting with retry logic
- ✅ Project structure organized for future features
- ✅ Documentation complete and accurate
- ✅ All functional requirements met
- ✅ All success criteria verified
- ✅ New developer can set up in <15 minutes

**Ready for Next Feature**: Feature 002 (User Authentication) can now proceed independently

---

## Summary

| Phase | Tasks | Purpose | Status |
|-------|-------|---------|--------|
| 1: Setup | T001-T006 | Project initialization | Ready to implement |
| 2: Foundational | T007-T014 | Docker, database, infrastructure | Blocking all stories |
| 3: US1 (P1) | T015-T023 | Developer setup flow | 🎯 MVP story |
| 4: US2 (P2) | T024-T030 | Backend module organization | Can start after Phase 2 |
| 5: US3 (P2) | T031-T042 | Frontend module organization | Can start after Phase 2 |
| 6: US4 (P2) | T043-T051 | Infrastructure documentation | Can start after Phase 2 |
| 7: Polish | T052-T060 | Documentation & verification | Completes feature |

**Parallel Execution**: 
- Phase 1 tasks are all parallelizable (T001-T006)
- Phase 2 mostly parallelizable (T007-T009, T011-T012) with one sequence point (T010, T014)
- Phases 3-6 can proceed in parallel after Phase 2; US2, US3, US4 have no dependencies on each other
- Phase 7 depends on all previous phases

**MVP Scope**: Phases 1-3 + Phase 7 provide the minimum viable product (dev environment setup). Phases 4-6 add infrastructure management and structure for subsequent features.

**Estimated Total Time**: 
- Sequential: 8-10 hours
- With parallelization: 5-6 hours
- Including manual testing & troubleshooting: 6-8 hours
